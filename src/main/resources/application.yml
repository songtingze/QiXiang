server:
    port: 8081

head:
#    uploadPath: "/root/App/photoUpload/"
    uploadPath: "D:/test/"
spring:
    datasource:
        driver-class-name: com.mysql.cj.jdbc.Driver
        url: jdbc:mysql://bj-cynosdbmysql-grp-94ctk06s.sql.tencentcdb.com:21171/app?characterEncoding=utf-8
        username: root
        password: Stz156184
    # redis配置
    redis:
        database: 1
        host: localhost
        # 端口，默认为6379
        port: 6379
        # 密码
#        password: xwtec123
        password:
        jedis:
            pool:
                max-active: 100
                max-idle: 10
                max-wait: 100000
        timeout: 5000
mybatis:
    type-aliases-package: com.example.app.entity
    mapper-locations: classpath:mybatis/dao/*.xml
#pagehelper分页插件配置
pagehelper:
    helper-dialect: mysql
    reasonable: true
    support-methods-arguments: true
